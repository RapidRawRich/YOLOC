# YOLOC Dependencies
# For fast installation with uv: uv pip install -r requirements.txt
# For standard pip: pip install -r requirements.txt (inside virtual environment)

# Core computer vision & ML
opencv-python>=4.8.0
ultralytics>=8.1.0
torch>=2.2.0
torchvision>=0.17.0

# Face recognition & OCR
face_recognition>=1.3.0
easyocr>=1.7.0

# Supporting utilities & build tools
numpy>=1.24.0
Pillow>=9.5.0
# setuptools pinned <82 for pkg_resources compatibility in face_recognition_models
setuptools<82

# Development & Type Checking
pyrefly>=1.3.0
